<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd">

    <http auto-config="true" use-expressions="true">
        <form-login login-processing-url="/resources/j_spring_security_check"
                    login-page="/login"
                    authentication-failure-url="/login?login_error=t"/>
        <logout logout-url="/resources/j_spring_security_logout" logout-success-url="/login"/>

        <intercept-url pattern="/ivrlanguages/**" access="hasRole('ADMIN')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/genders/**" access="hasRole('ADMIN')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/clinics/**" access="hasRole('ADMIN')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/clinicians/**" access="hasRole('ADMIN')" requires-channel="${web.protocol}"/>
	    <intercept-url pattern="/patients/**" access="hasAnyRole('CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR','ADMIN')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/patients" access="hasAnyRole('CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" method="POST" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/companys/**" access="hasAnyRole('ADMIN','CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/brands/**" access="hasAnyRole('ADMIN','CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/drugs/**" access="hasAnyRole('ADMIN','CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/dosagetypes/**" access="hasAnyRole('ADMIN','CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/regimens/**" access="hasAnyRole('ADMIN','CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/mealadvicetypes/**" access="hasAnyRole('ADMIN','CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/treatmentadvices/**" access="hasAnyRole('ADMIN','CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" requires-channel="${web.protocol}"/>
        <intercept-url pattern="/" access="hasAnyRole('ADMIN','CLINICIAN_STUDY_NURSE','CLINICIAN_DOCTOR')" requires-channel="${web.protocol}"/>
    </http>

    <beans:bean name="authenticationProvider" class="org.motechproject.tama.security.AuthenticationProvider">
        <beans:constructor-arg>
            <beans:list>
                <beans:ref bean="clinicianGroup"/>
                <beans:ref bean="adminGroup"/>
            </beans:list>
        </beans:constructor-arg>
    </beans:bean>

    <beans:bean id="clinicianGroup" class="org.motechproject.tama.security.profiles.ClinicianSecurityGroup"/>
    <beans:bean id="adminGroup" class="org.motechproject.tama.security.profiles.AdminSecurityGroup"/>

    <authentication-manager alias="authenticationManager">
        <authentication-provider ref="authenticationProvider"/>
    </authentication-manager>

</beans:beans>